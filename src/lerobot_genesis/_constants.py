"""Shared constants with no third-party imports (so any module can use them dependency-free)."""

from __future__ import annotations

#: Gymnasium namespace this package registers reference tasks under (see lerobot_genesis.tasks).
GYM_NAMESPACE = "lerobot_genesis"
